# Copy to .env to override compose defaults. docker compose reads .env
# automatically; it is gitignored.

# Host port the app is served on.
WEB_PORT=8080

# Backend log level: debug, info, warn, error.
LOG_LEVEL=info

# Stamped into the binary and reported by `/server -version`.
VERSION=dev
